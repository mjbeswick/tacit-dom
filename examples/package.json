{
  "name": "reactive-dom-examples",
  "version": "1.0.0",
  "description": "Examples for the reactive-dom library",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --config vite.config.ts",
    "preview": "vite preview --config vite.config.ts",
    "build": "vite build --config vite.config.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "reactive-dom": "file:.."
  }
} 